{
  "name": "local-mcp",
  "version": "1.1.0",
  "description": "Local file system MCP server — read, edit, search, exec tools for AI assistants via stdin/stdout",
  "type": "module",
  "main": "local-mcp.mjs",
  "bin": {
    "local-mcp": "./local-mcp.mjs"
  },
  "scripts": {
    "start": "node local-mcp.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {},
  "engines": {
    "node": ">=22.0.0"
  },
  "files": [
    "local-mcp.mjs",
    "lib/",
    "scripts/"
  ],
  "keywords": ["mcp", "model-context-protocol", "filesystem", "ai", "claude"],
  "license": "MIT"
}
